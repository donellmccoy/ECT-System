<?xml version="1.0"?>

<configuration>

	<system.web>
		<authorization>
			<!-- deny unauthenticated users -->
			<deny users="?" />
			<!-- Allow everybody else -->
			<allow users="*" />
		</authorization>
	</system.web>
</configuration>